#{init => init_state,
  map =>
      #{state5_std => #{recv => #{after_5s => {stop_state,[]}}},
        state1_send_after => #{send => #{before_5s => {stop_state,[]}}}},
  timeouts => #{},
  resets => #{unresolved => #{}},
  timers => #{}}.

#{init => init_state,
  map => #{state2_std => #{send => #{after_t => {stop_state,[]}}}},
  timeouts => #{},
  resets =>
      #{state1_unexpected_timer_start_state => #{t => 5000},
        {state2_std,1,resolved} => 5000},
  states_to_resolve =>
      #{2 => {state1_unexpected_timer_start_state,1,unresolved}},
  timers => #{}}.

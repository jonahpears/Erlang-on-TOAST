#{init => init_state,
  map =>
      #{state4_std => #{recv => #{after_5s => {stop_state,[]}}},
        state1_recv_after => #{recv => #{before_5s => {stop_state,[]}}}},
  timeouts => #{},
  resets => #{unresolved => #{}},
  timers => #{}}.

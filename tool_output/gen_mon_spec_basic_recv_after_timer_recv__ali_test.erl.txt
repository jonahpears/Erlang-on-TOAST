#{init => state2_recv_after,
  map =>
      #{state2_recv_after => #{recv => #{before_5s => stop_state}},
        state5_std => #{recv => #{after_5s => stop_state}}},
  timeouts => #{state2_recv_after => {t,standard_state}},
  resets => #{init_state => #{t => 5000}},
  timers => #{}}.

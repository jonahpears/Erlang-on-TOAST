#{init => state1_unexpected_timer_start_state,
  map =>
      #{state3_std => #{send => #{finished => {stop_state,[]}}},
        state6_std => #{send => #{data => {state2_if_else,[]}}}},
  timeouts => #{},
  resets => #{init_state => #{t => 5000}},
  timers => #{}}.

#{init => init_state,
  map =>
      #{state2_send_after => #{send => #{first => {state3_recv_after,[]}}},
        state3_recv_after => #{recv => #{second => {stop_state,[]}}},
        state9_recv_after => #{recv => #{second => {stop_state,[]}}},
        state11_std => #{send => #{third => {stop_state,[]}}}},
  timeouts => #{},
  resets => #{init_state => #{t1 => 5000}},
  timers => #{}}.
